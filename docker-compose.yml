version: "3.8"

services:
  tempmail:
    build: .
    container_name: diaastore-tempmail
    restart: always
    network_mode: host
    environment:
      - IMAP_HOST=127.0.0.1
      - IMAP_PORT=993
      - IMAP_USER_1=inbox@diaa.store
      - IMAP_PASS_1=Diaatemp2026
      - MAIL_DOMAIN_1=diaa.store
      - IMAP_USER_2=inbox@diaastore.cloud
      - IMAP_PASS_2=TempMail2026
      - MAIL_DOMAIN_2=diaastore.cloud
      - PORT=3000
